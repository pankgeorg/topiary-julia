# Minimal tree-sitter-julia parse failures.
#
# Generated by `corpus/minimize_gaps.jl`. Do not hand-edit — regenerate
# the script's output and commit the whole file.
#
# Consumed by `build.rs`, which emits one `#[test]` per entry. Each
# test asserts tree-sitter-julia still errors on the snippet; when a
# snippet starts parsing clean, the test fails with `resolved` so the
# entry gets removed here.

[[snippet]]
js_sexp = "@$"
code = "$"
origin = "corpus/fixtures/parse_errors.jl:20"
[[snippet]]
js_sexp = "(vcat a)"
code = """[a\n  ;]"""
origin = "corpus/fixtures/parse_errors.jl:14"
[[snippet]]
js_sexp = "(call-i public + 3)"
code = "public + 3"
origin = "corpus/fixtures/parse_errors.jl:7"
[[snippet]]
js_sexp = "(vect (= x 1) (parameters (= y 2)))"
code = "[x=1, ; y=2]"
origin = "corpus/fixtures/parse_errors.jl:17"
[[snippet]]
js_sexp = "(let (block (= x outer)) (error-t i = rhs) (block body))"
code = """let x = outer i = rhs\n    body\nend"""
origin = "corpus/fixtures/parse_errors.jl:10"
