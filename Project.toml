# Julia dependency manifest for topiary-julia test corpus.
#
# This file documents the Julia packages whose test data we use.
# It is NOT a runtime dependency — topiary-julia is a pure Rust project.
# Use this to track versions and reproduce the test environment.
#
# To instantiate (optional, for corpus validation with Julia):
#   julia --project=. -e 'using Pkg; Pkg.instantiate()'

name = "Topiary Julia Tests"
uuid = "00000000-0000-0000-0000-000000000001"

[deps]
JuliaSyntax = "70703baa-626e-46a2-a12c-08ffd08c73b4"

[compat]
JuliaSyntax = "1.0"
julia = "1.10"
